---
import PostSingleGuide from "@/partials/PostSingleGuide.astro";
import PostSingleWriting from "@/partials/PostSingleWriting.astro";

const { post } = Astro.props;
const isGuide = post.data.preset === "guide";
---

{isGuide ? <PostSingleGuide post={post} /> : <PostSingleWriting post={post} />}
